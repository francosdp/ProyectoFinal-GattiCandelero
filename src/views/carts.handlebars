<div>
  <h1>Tu carrito</h1>
  {{#each cart}}
    <div>
      
      <h4>{{this.product.title}}</h4>
      <p>Precio por unidad: {{this.product.price}}</p>
      <p>Unidades: {{this.quantity}}</p>

      
    </div>
  {{/each}}
 
</div>